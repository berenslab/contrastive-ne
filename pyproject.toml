[project]
name = "cne"
version = "0.1.2"
description = "Contrastive Neighbor Embedding for low-dimensional distance-less embeddings"
authors = [
    {name = "Niklas Böhm", email = "jan-niklas.boehm@uni-tuebingen.de"},
]
dependencies = [
    "torch~=1.10",
    "numpy>=1.17,<2",
    "scikit-learn~=1.0"
]
requires-python = ">=3.8,<3.10"
license = {text = "EUPL"}

[project.urls]
homepage = ""

[tool]
[tool.pdm]

[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"
